export const docSourceList = ['告警平台', '事件管理'].map((item, idx) => ({
  label: item,
  key: idx + 1,
  value: idx + 1
}));

export const docsTypeList = ['公共知识', '私有知识'].map((item, idx) => ({
  label: item,
  key: idx + 1,
  value: idx + 1
}));
